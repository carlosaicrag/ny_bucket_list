extends layout.pug 

block content 
  div welcome to the bucket list! 

  form(action="/users/login", method="post") 
    input(placeholder="username", name="username")
    input(placeholder="password", name="password")

    button(type="submit" class="nav-link") Login